#include <stdio.h>
#include <math.h>
#include <stdio.h>
 
int main(){
  unsigned long long n;
  int t;
  printf("Nhap vao so can kiem tra = ");
  scanf("%llu", &n);
  printf("Nhap vao tham so an toan ");
  scanf("%d", &t);
  return 0;
}
